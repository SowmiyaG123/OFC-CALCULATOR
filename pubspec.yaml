name: ftth_planner_pro
description: FTTH Planning and Calculation App

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.14
  screenshot: ^3.0.0
  supabase_flutter: ^2.5.0 # 👈 Added this line for Supabase
  image_gallery_saver_plus: ^4.0.1
  intl: ^0.19.0
  share_plus: ^7.2.1
  xml: ^6.3.0 # ✅ ADD THIS LINE
  image: ^4.1.3
  razorpay_flutter: ^1.3.7
  app_links: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo_pic.png
